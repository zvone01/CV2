function c = filter_by_sampson_distance(c)

F = get_fundamental_mat(c)

% determine sampson distance for each correspondence
% filter out those correspondences with d > t
% return only correspondences with d < t


end